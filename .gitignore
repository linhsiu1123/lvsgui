/node_modules
/.next
/out
/build
/coverage

# env / secrets
.env
.env*.local

next-env.d.ts
*.tsbuildinfo
.DS_Store

# Tooling state. `.omc/*` contains a slash, so git anchors it to this
# directory and it misses nested copies such as backend/.omc — hence the
# unanchored form.
.omc/